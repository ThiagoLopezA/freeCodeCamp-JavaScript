// Escaping Literal Quotes in Strings

const myStr = 'I am a "double quoted" string inside "double quotes".';

// That's one way but in this case the right way is:
// const myStr = "I am a \"double quoted\" string inside \"double quotes\"."
// Using \ before the " to ignore it
